[app]

# (str) Title of your application
title = Scanner

# (str) Package name
package.name = scanner

# (str) Package domain (needed for android packaging)
package.domain = ru.scanner

# (str) Source code where the main.py lives
source.dir = .

# (list) Source files to include (добавлен json по вашему запросу)
source.include_exts = py,png,jpg,kv,json,ttf

# (str) Application versioning
version = 1.0.0

# (list) Application requirements
# Оставлен автоматический подбор версии Python сборщиком
requirements = python3,kivy,setuptools,openpyxl

# (str) Supported orientations (landscape, portrait or all)
orientation = portrait

# (bool) Indicate if the application should be fullscreen or not
fullscreen = 0

# ==============================================================================
# Android specific settings
# ==============================================================================

# (list) Permissions
android.permissions = READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE

# (int) Target Android API
android.api = 33

# (int) Minimum API your APK will support (Android 7.0)
android.minapi = 24

# (str) Android NDK version to use
android.ndk = 25b

# (bool) If True, then automatically accept SDK license agreements.
android.accept_sdk_license = True

# (str) The Android architectural architecture to target.
android.archs = armeabi-v7a, arm64-v8a

# Отключаем ccache во избежание конфликтов линковщика графики
android.ccache = 0

# ==============================================================================
# Buildozer specific settings
# ==============================================================================
[buildozer]

# (int) Log level (0 = error only, 1 = info, 2 = debug)
log_level = 2

# (int) Display warning if buildozer is run as root
warn_on_root = 0